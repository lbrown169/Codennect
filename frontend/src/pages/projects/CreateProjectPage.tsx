import { Title, Box } from '@mantine/core';

export default function CreateProjectPage() {
    return (
        <Box mx={{ base: 'md', lg: 'xl' }}>
            <Title py="md" order={1}>
                Create Project
            </Title>
        </Box>
    );
}
