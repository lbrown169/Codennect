import { Title, Box } from '@mantine/core';

export default function TeamUsersPage() {
    return (
        <Box mx={{ base: 'md', lg: 'xl' }}>
            <Title py="md" order={1}>
                My Teams
            </Title>
        </Box>
    );
}
