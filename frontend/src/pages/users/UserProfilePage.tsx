import { Title, Box } from '@mantine/core'

export default function UserProfilePage() {
    return (
        <Box mx={{ base: 'md', lg: 'xl' }}>
            <Title py="md" order={1}>
                User Profile
            </Title>
        </Box>
    )
}
